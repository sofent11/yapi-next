## 研发环境说明（Next 架构）

本文档原内容基于旧版 Koa/ykit 栈，现已归档。

当前开发方式：

```bash
npm install
npm run next:api:dev
npm run next:web:dev
```

构建与回归：

```bash
npm run next:build
npm run next:smoke:api
npm run next:self-assess:api
```
