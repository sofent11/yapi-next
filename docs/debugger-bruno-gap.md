# YAPI Debugger vs Bruno OSS Gap

Reference baseline:

- Bruno main clone: `/Users/sofent/work/_references/bruno-main`
- Bruno v3.3.0 clone: `/Users/sofent/work/_references/bruno-v3.3.0`
- Bruno OSS tag inspected: `v3.3.0` (`34460d5`)

This document tracks parity against Bruno OSS while keeping YAPI Debugger's local-first YAML workspace format.

## Current Parity Matrix

| Area | Bruno OSS capability | YAPI Debugger status | Notes |
|---|---|---|---|
| HTTP requests | Method, URL, params, headers, body, auth | Implemented | Existing workbench covers the primary REST flow. |
| Body modes | JSON, text, XML, GraphQL, SPARQL, file, form URL encoded, multipart | Partial | Schema/UI/runtime now accept XML, GraphQL, SPARQL, and file body for HTTP send. GraphQL query variables are materialized as JSON payloads, and the UI can fetch introspection, cache the schema summary on the request, clear stale cache entries, preserve that cache through Bruno JSON/OpenCollection JSON round-trips, generate query/mutation/subscription drafts from root fields with scalar, enum, list, and input-object variable placeholders, format variables JSON in-place, and save/load reusable operation snapshots on the request. The debugger now also exposes a selectable GraphQL explorer with child-field toggles, root-field schema filtering, and insert-time fragment style control so selected interface/union branches can be emitted as reusable named fragments instead of only inline spreads. Deeper schema navigation polish is still open. |
| Request kinds | HTTP, GraphQL, gRPC, WebSocket, JS | Partial | Schema has `kind: http/graphql/grpc/websocket/script`; GraphQL sends over HTTP POST. WebSocket now has batch connect/send/receive, live connect/send/close plus reconnect controls, persists the most recent timeline on the request, supports json/text/base64 binary outgoing frames, preserves named saved examples on the request, and exposes richer live-event previews for json/text/base64 payloads. Script items now execute natively in the debugger sandbox with a dedicated body editor, runtime logs/tests, variable mutations, and lite `pm.sendRequest` orchestration. gRPC requests now execute natively through the desktop bridge for unary plus workspace-authored **server-streaming** methods by compiling the configured proto/import paths at runtime, resolving interpolated service/method/message fields, and feeding the decoded response into the existing response history/viewer pipeline (server streams are aggregated into a JSON result with message count metadata). Runtime fixtures now cover both request resolution and aggregated server-streaming/native-response script assertions so regressions stay visible. Client/bidi streaming and broader session authoring polish are still pending. |
| Auth | Basic, bearer, API key, OAuth2, OAuth1, AWS v4, Digest, NTLM, WSSE | Partial | Schema/UI preserve all major Bruno auth families. Runtime signing is implemented for basic/bearer/API key, cached OAuth2 tokens, OAuth1 HMAC-SHA1/PLAINTEXT headers or query params, AWS Signature v4 headers, Digest challenge retry, WSSE UsernameToken headers, plus NTLM Type 1 negotiate headers and Type 2 challenge → Type 3 retry continuation on the current runtime session. The remaining NTLM gap is now explicit: the current Tauri desktop bridge sends HTTP through `reqwest` with `rustls`, so it supports only explicit username/password NTLM credentials (optionally with domain/workstation). Native/integrated enterprise flows such as SSPI, GSSAPI, or Negotiate/Kerberos are not available without a dedicated platform auth bridge. |
| Variables | Collection/folder/request/response/env/global/prompt vars | Partial | Schema has scoped variable rows. Request-scoped variable rows now resolve through preview/runtime, prompt-scoped request rows ask for values right before send/auth refresh, the debugger now remembers prompt values per workspace and lets users edit or clear those defaults from Environment Center, and category folders persist `_folder.yaml` variable documents that feed request preview/runtime plus collection runs with parent-folder inheritance and child-folder override behavior. Environment Center now also exposes an active-request precedence audit so authors can see which layer wins, which duplicate values are shadowed, and where to edit the winning source without changing runtime semantics. Broader variable-management workflows are still lighter than Bruno. |
| Scripts/tests | Pre-request, post-response, tests, assertions | Partial | Existing case scripts and checks remain. The request editor now exposes reusable request-level pre-request / post-response / tests blocks, the collection designer now exposes collection-level script hooks, runtime execution now runs **collection → request → case** script layers in order, and the script sandbox now covers richer helpers such as `pm.collectionVariables`, `pm.iterationData`, `pm.info`, mutable `pm.request` header/query/body helpers, response status/header/cookie helpers, `pm.cookies`, `pm.response.json(path?)`, `pm.response.reason()/responseTime/responseSize`, plus deeper `pm.expect(...).to.have.lengthOf/property` and boolean assertions. The local runner now also honors `pm.execution.skipRequest()` for pre-request skips plus limited `pm.execution.setNextRequest()` / `postman.setNextRequest()` collection flow control for forward-only step key/name/request jumps or `null` stop, while `inspectResolvedRequest` warns about those limits and still flags unsupported migration hazards such as `pm.globals`, `pm.visualizer`, and `pm.require` before send. Collection/request tests feed the existing response and runner check pipeline. Full Bruno JS API parity is still pending. |
| Runner | GUI runner, tags, env matrix, reports | Implemented | Existing collection runner supports iteration, env matrix, retries, reports. GUI and CLI can filter runs by tags across step/request/case metadata, collection `runnerTags` act as default filters, reports expose active filters, failed reruns inherit report filters, collections can persist reusable **run presets** for environment/tag/step-key/fail-fast launch profiles directly in the designer, and the designer now exposes collection reporter selection that drives which report export actions stay enabled. The standalone `yapi-debugger-runner` CLI now ships outside the GUI surface, can list/apply saved presets, and can export **all enabled reporter formats in one action** from the configured reporter set. |
| Import/export | Bruno, Postman, OpenAPI, Insomnia, WSDL, OpenCollection, env | Partial | Existing OpenAPI/Postman/HAR remain. Bruno single `.bru` HTTP request import/export maps method/url/query/path params/headers/body/auth/scripts/tests/docs across the local workspace model. Selected collections can export/import a Bruno folder (`bruno.json`, `collection.bru`, `folder.bru`, request `.bru` files, `environments/*.bru`) into requests, environments, and a runnable YAPI Collection. Bruno JSON collections now import folders, HTTP/GraphQL/WebSocket/gRPC/script items, and runnable collection steps, covering Bruno's WSDL JSON converter output; debugger collections can also serialize to Bruno JSON with folder nesting and steps for round-trip fixtures, and gRPC requests now preserve proto path, import paths, service/method, message, and the supported `server-streaming` RPC kind across that round-trip so the native runtime path stays runnable after export/import. Insomnia v4 JSON import maps workspaces/folders/requests/environments plus common body/auth shapes. OpenCollection YAML/JSON import maps HTTP, GraphQL, WebSocket, gRPC, script items, nested folders, environments, params, headers/metadata, body/message payloads, auth, request vars, scripts/tests/assertion notes, docs, and HTTP response examples. Debugger collections can now serialize to OpenCollection JSON with nested folders, environments, scripts, variables, and mixed request kinds, and gRPC requests likewise retain proto/import/service/method/message plus the supported `server-streaming` RPC kind through that round-trip; the desktop Collection designer exposes the OpenCollection export action. Broader gRPC client/bidi streaming and richer session authoring still remain preservation-first gaps. |
| Response viewer | JSON/XML/HTML/text/binary/media/PDF, search, download | Implemented | Existing body/json/headers/cookies/compare/raw remain. The debugger now keeps a base64 copy of live response bytes, exposes a dedicated preview tab, renders HTML, image, audio, video, PDF, and binary-oriented previews based on content type, allows direct payload download, adds response-wide search across body/JSON/headers/cookies, deepens compare mode with diff breakdown stats, added/removed/changed filters, inline line-level highlights, provides a navigable side-by-side merge-style workbench for live-vs-example review, and now also supports editor-level full-text highlighting plus next/previous navigation inside the response body editors. |
| Git | Clone, status, visual diff, pull/push | Implemented | Existing status/pull/push/open terminal remain. The Git Helper now lets users select changed files and inspect their current diff inside the debugger via a new Tauri `git_diff` bridge command, and the welcome flow can now clone a repository into a chosen parent directory, stream live `git clone --progress` updates in-app, fail fast without hanging on terminal prompts, surface actionable auth guidance for private remotes, and then auto-open the cloned workspace. |
| Preferences | Theme, font, zoom, keybindings, proxy, certs, cache | Partial | A dedicated Preferences center now manages theme, UI zoom, code font size, safe keybinding presets plus configurable high-frequency shortcuts for command palette/save/run/environment center/preferences, runtime proxy/certificate defaults, and local cache clearing. Dark mode now propagates through debugger CSS, Mantine components, and CodeMirror. Broader app-wide keybinding breadth is improved, though still intentionally smaller than Bruno’s full shortcut surface. |
| UI density | Mature API IDE layout | Implemented | The debugger workbench now lands on a calmer, denser finish: request/response chrome is grouped more deliberately, Environment Center and Preferences expose compact top-level summaries, and the newer parity surfaces share tighter spacing plus clearer information hierarchy. |

## Implementation Guardrails

- Do not vendor Bruno source into this repository.
- Keep YAPI Debugger's workspace format as the durable source of truth.
- Preserve existing YAPI-specific flows: import repair, browser capture, cases, collection reports, and Git helper.
- Any Bruno-parity item that is schema/UI-only must be documented as pending runtime support until it can send, import/export, and test successfully.

## Next Milestones

1. Reassess remaining partial parity items after the first gRPC streaming runtime milestone: full Bruno JS API parity, gRPC client/bidi streaming plus richer session authoring, and the remaining GraphQL/editor polish that still goes beyond saved drafts, reusable fragment generation, and root-field schema filtering.
2. Treat future desktop polish as incremental maintenance while prioritizing deeper script/runtime parity and the remaining gRPC/session gaps.
