import { CompatWorkspace } from './shared/CompatWorkspace';

export function CompatConsolePage() {
  return (
    <CompatWorkspace
      description="Compat Console：兼容接口回归验证页（`open/*`、`log/*`、`test/*`、`interface/interUpload`）。"
      requestSource="compat-console"
    />
  );
}
