import { CaseWorkspace } from './shared/CaseWorkspace';

export function CaseConsolePage() {
  return (
    <CaseWorkspace
      description="Case Console：测试集合与测试用例管理（`col/*`），包含脚本执行、环境视图、变量视图。"
    />
  );
}
