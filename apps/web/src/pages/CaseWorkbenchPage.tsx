import { CaseWorkspace } from './shared/CaseWorkspace';

export function CaseWorkbenchPage() {
  return (
    <CaseWorkspace
      title="测试集合 / Case 工作台"
      descriptionType="secondary"
      description="覆盖 `col/*` 迁移接口：集合管理、用例管理、变量视图、环境视图、脚本执行验证。"
    />
  );
}
